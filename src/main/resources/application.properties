# Application metadata
spring.application.name=zai-weather-service

# Server configuration
server.port=8080

# API Keys (with optional fallbacks)
weatherstack.api.key=${WEATHERSTACK_API_KEY:dummy-weatherstack-key}
openweathermap.api.key=${OPENWEATHERMAP_API_KEY:dummy-openweather-key}

# Cache config
weather.cache.ttl=3000
